import { useEffect, useState } from "react";
import api from "../api";
import SandboxCheck from "../components/admin/SandboxCheck";

function CheckList({ title, checks = [] }) { return <div style={{ marginTop: 18 }}><h3 style={{ fontFamily: "var(--serif)" }}>{title}</h3>{checks.map((c) => <SandboxCheck key={c.name} check={c} />)}</div>; }
function SuiteSummary({ result }) { if (!result) return null; const checks = [...(result.health?.checks || []), ...(result.features?.checks || [])]; const passed = checks.filter((c) => c.passed).length; return <div style={{ marginTop: 18, padding: 14, border: "1px solid var(--border)", borderRadius: 10 }}><strong>{result.status === "ready" ? "✓ Sandbox ready" : "✕ Sandbox needs attention"}</strong><div style={{ color: "var(--ink-faint)", fontSize: 13, marginTop: 4 }}>{passed}/{checks.length} readiness checks passed · Automatic email: {result.automatic_email?.would_send ? "WOULD SEND" : "would not send"}</div></div>; }
function SimulationRows({ users = [] }) { return <div style={{ marginTop: 18 }}><h3 style={{ fontFamily: "var(--serif)" }}>Email simulation</h3>{users.map((u) => <div key={u.user_id} style={{ padding: 10, borderBottom: "1px solid var(--border)", fontSize: 13 }}><strong>User #{u.user_id}</strong> · {u.timezone} · configured {u.configured_send_time} · approved: {u.approved_stories} · selected: {u.selected_stories} · <strong>{u.would_send ? "WOULD SEND" : `would not send (${u.decision})`}</strong>{u.selected_headlines?.length > 0 && <div style={{ marginTop: 5, color: "var(--ink-faint)" }}>Selected: {u.selected_headlines.join(" · ")}</div>}</div>)}</div>; }
function StatusPill({ status }) { const labels = { scheduled: "Scheduled", waiting: "Waiting", in_progress: "In Progress", completed: "Sent", failed: "Failed", idle: "Ready" }; return <span style={{ fontSize: 11, fontWeight: 700, padding: "5px 10px", borderRadius: 14, border: "1px solid var(--border)", background: status === "completed" ? "#eef7f0" : status === "failed" ? "#fff0ed" : status === "in_progress" ? "#fff8df" : "var(--surface)" }}>{labels[status] || status}</span>; }
function TimelineStep({ title, status, detail, time, last = false }) { return <div style={{ display: "flex", gap: 12, minHeight: last ? 48 : 64 }}><div style={{ width: 22, display: "flex", flexDirection: "column", alignItems: "center" }}><div style={{ width: 16, height: 16, borderRadius: "50%", border: "2px solid var(--border)", background: status === "completed" ? "#2e7d4f" : status === "failed" ? "#c0392b" : status === "in_progress" ? "#b8860b" : "var(--surface)" }} />{!last && <div style={{ width: 2, flex: 1, background: "var(--border)", marginTop: 3 }} />}</div><div style={{ flex: 1, paddingBottom: 10 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 8 }}><strong>{title}</strong><StatusPill status={status} /></div><div style={{ fontSize: 12.5, color: "var(--ink-faint)", marginTop: 3 }}>{detail}</div>{time && <div style={{ fontSize: 11, color: "var(--ink-faint)", marginTop: 2 }}>{time}</div>}</div></div>; }
function TestDeliveryStatus({ schedule, refreshKey }) { const [job, setJob] = useState(null); const load = async () => { try { const r = await api.get("/admin/jobs", { timeout: 15000 }); setJob(r.data?.test_email || null); } catch {} }; useEffect(() => { load(); const id = setInterval(load, 2000); return () => clearInterval(id); }, [refreshKey]); const status = job?.status || (schedule?.enabled ? "scheduled" : "idle"); const target = schedule?.scheduled_at ? new Date(schedule.scheduled_at) : null; const remaining = target ? Math.max(0, target.getTime() - Date.now()) : 0; const countdown = remaining > 0 ? remaining < 3600000 ? `${Math.ceil(remaining / 60000)} min` : `${Math.floor(remaining / 3600000)} hr ${Math.floor((remaining % 3600000) / 60000)} min` : null; return <div style={{ marginTop: 18, padding: 18, border: "1px solid var(--border)", borderRadius: 10 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10 }}><h3 style={{ fontFamily: "var(--serif)", margin: 0 }}>Automatic test delivery status</h3><StatusPill status={status} /></div>{status === "scheduled" && target && <div style={{ fontWeight: 700, marginTop: 10 }}>Scheduled → will send in {countdown || "less than a minute"}</div>}<div style={{ marginTop: 14 }}><TimelineStep title="Scheduled" status={status === "completed" ? "completed" : status === "in_progress" ? "completed" : schedule?.enabled ? "scheduled" : "idle"} detail={target ? `Target: ${target.toLocaleString()}` : "No test email scheduled."} /><TimelineStep title="Sending" status={status === "in_progress" ? "in_progress" : status === "completed" ? "completed" : "idle"} detail={status === "in_progress" ? "Sending only to the configured developer/test address…" : "Waiting for the scheduled time."} time={job?.started_at} /><TimelineStep last title="Sent" status={status === "completed" ? "completed" : status === "failed" ? "failed" : "idle"} detail={status === "completed" ? "Test email sent successfully." : status === "failed" ? (job?.error || "Test email failed.") : "Not sent yet."} time={job?.completed_at} /></div></div>; }

export default function Sandbox() {
  const [result, setResult] = useState(null), [busy, setBusy] = useState(false), [error, setError] = useState(""), [simulationDate, setSimulationDate] = useState(""), [schedule, setSchedule] = useState(null), [testHour, setTestHour] = useState("12"), [testMinute, setTestMinute] = useState("00"), [refreshKey, setRefreshKey] = useState(0);
  const run = async (path) => { setBusy(true); setError(""); setResult(null); try { const res = await api.get(path, { timeout: 60000 }); setResult(res.data); } catch (err) { setError(err.friendlyMessage || "Sandbox check failed"); } finally { setBusy(false); } };
  const sendSafeTest = async () => { if (!confirm("Send one test email to the configured developer/test address only? No real subscriber will receive it.")) return; setBusy(true); setError(""); try { const res = await api.post("/admin/actions/send-test-email", null, { timeout: 30000 }); setResult({ status: "in_progress", ...res.data, safe: true }); setRefreshKey((x) => x + 1); } catch (err) { setError(err.friendlyMessage || "Test email failed"); } finally { setBusy(false); } };
  const loadSchedule = async () => { try { const res = await api.get("/admin/sandbox/test-email-schedule"); setSchedule(res.data); } catch (err) { setError(err.friendlyMessage || "Could not load test schedule"); } };
  useEffect(() => { loadSchedule(); }, []);
  const scheduleTest = async () => { const hour = Number(testHour), minute = Number(testMinute); if (!Number.isInteger(hour) || !Number.isInteger(minute) || hour < 0 || hour > 23 || minute < 0 || minute > 59) { setError("Enter a valid time in 24-hour HH:MM format."); return; } if (!confirm(`Schedule a SAFE test email for ${String(hour).padStart(2, "0")}:${String(minute).padStart(2, "0")}? It will go only to the configured developer/test address.`)) return; setBusy(true); setError(""); try { const res = await api.post(`/admin/sandbox/test-email-schedule?hour=${hour}&minute=${minute}`, null, { timeout: 30000 }); setSchedule(res.data); setRefreshKey((x) => x + 1); } catch (err) { setError(err.friendlyMessage || "Could not schedule test email"); } finally { setBusy(false); } };
  const cancelSchedule = async () => { setBusy(true); setError(""); try { const res = await api.delete("/admin/sandbox/test-email-schedule"); setSchedule(res.data); setRefreshKey((x) => x + 1); } catch (err) { setError(err.friendlyMessage || "Could not cancel test email"); } finally { setBusy(false); } };
  const runSimulation = () => { if (!/^\d{4}-\d{2}-\d{2}$/.test(simulationDate)) { setError("Choose an edition date first."); return; } run(`/admin/sandbox/simulate?edition_date=${encodeURIComponent(simulationDate)}`); };
  return <div className="admin-page"><div className="admin-title">Production Sandbox</div><p style={{ color: "var(--ink-faint)", maxWidth: 760 }}>Run safe readiness checks before deployment. Simulations are read-only. Test emails go only to the configured developer/test address and never to subscribers.</p><div style={{ display: "flex", gap: 10, flexWrap: "wrap", marginTop: 18 }}><button className="btn-secondary" disabled={busy} onClick={() => run("/admin/sandbox/suite")}>{busy ? "Running..." : "Run full sandbox suite"}</button><button className="btn-secondary" disabled={busy} onClick={() => run("/admin/sandbox/health")}>System health</button><button className="btn-secondary" disabled={busy} onClick={() => run("/admin/sandbox/features")}>Feature readiness</button><button className="btn-secondary" disabled={busy} onClick={() => run("/admin/sandbox/automatic-email")}>Simulate automatic email</button><button className="btn-secondary" disabled={busy} onClick={sendSafeTest}>Send safe test email</button></div><div style={{ marginTop: 28, padding: 18, border: "1px solid var(--border)", borderRadius: 10 }}><h3 style={{ fontFamily: "var(--serif)", marginTop: 0 }}>Test an existing approved edition</h3><p style={{ color: "var(--ink-faint)", fontSize: 13 }}>Pick a date such as 2026-08-31 to verify that the same personalization logic selects the best configured number of stories. Nothing is sent.</p><div style={{ display: "flex", gap: 10, alignItems: "center", flexWrap: "wrap" }}><input type="date" value={simulationDate} onChange={(e) => setSimulationDate(e.target.value)} /><button className="btn-secondary" disabled={busy} onClick={runSimulation}>Simulate this edition</button></div></div><div style={{ marginTop: 28, padding: 18, border: "1px solid var(--border)", borderRadius: 10 }}><h3 style={{ fontFamily: "var(--serif)", marginTop: 0 }}>Schedule a safe automatic test email</h3><p style={{ color: "var(--ink-faint)", fontSize: 13 }}>Enter <strong>hour and minute in 24-hour format</strong>. The backend triggers the test at the configured minute; it no longer waits for GitHub Actions polling.</p><div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}><input aria-label="Test email hour" type="number" min="0" max="23" value={testHour} onChange={(e) => setTestHour(e.target.value)} style={{ width: 70 }} /><span>:</span><input aria-label="Test email minute" type="number" min="0" max="59" value={testMinute} onChange={(e) => setTestMinute(e.target.value)} style={{ width: 70 }} /><button className="btn-secondary" disabled={busy} onClick={scheduleTest}>Schedule test email</button>{schedule?.enabled && <button className="btn-secondary" disabled={busy} onClick={cancelSchedule}>Cancel</button>}</div>{schedule?.enabled && <div style={{ marginTop: 10, fontSize: 13 }}><strong>Scheduled:</strong> {schedule.scheduled_at} · one-shot · safe</div>}{schedule?.last_result && <div style={{ marginTop: 8, color: "var(--ink-faint)", fontSize: 13 }}><strong>Last result:</strong> {schedule.last_result}</div>}{!schedule?.recipient_configured && <div style={{ marginTop: 8, color: "var(--ink-faint)", fontSize: 13 }}>Configure <code>developer_test_email</code> in Admin → Settings first.</div>}</div><TestDeliveryStatus schedule={schedule} refreshKey={refreshKey}/>{busy && <div className="loader" style={{ padding: "30px 0" }}>Running sandbox check...</div>}{error && <div className="error-text">{error}</div>}{result && <div style={{ marginTop: 24, background: "var(--surface)", border: "1px solid var(--border)", borderRadius: 10, padding: 18 }}><div style={{ fontWeight: 700 }}>Result: {result.status || "completed"}</div><SuiteSummary result={result}/>{result.checks && <CheckList title="Checks" checks={result.checks}/>} {result.health?.checks && <CheckList title="System health" checks={result.health.checks}/>} {result.features?.checks && <CheckList title="Feature readiness" checks={result.features.checks}/>} {result.users && <SimulationRows users={result.users}/>} {result.inspected_users && <SimulationRows users={result.inspected_users}/>} {result.automatic_email?.inspected_users && <SimulationRows users={result.automatic_email.inspected_users}/>} {result.detail && <div style={{ marginTop: 10, color: "var(--ink-faint)" }}>{result.detail}</div>}<details style={{ marginTop: 18 }}><summary>Raw response</summary><pre style={{ whiteSpace: "pre-wrap", fontSize: 12, marginTop: 10 }}>{JSON.stringify(result, null, 2)}</pre></details></div>}</div>;
}